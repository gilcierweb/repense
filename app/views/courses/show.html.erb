<p>
  <strong><%= t('courses.name') %>:</strong>
  <%= @course.name %>
</p>

<p>
  <strong><%= t('courses.description') %>:</strong>
  <%= @course.description %>
</p>

<p>
  <strong><%= t('students.status') %>:</strong>
  <%= text_status(@course.status) %>
</p>

<%= link_to t('views.btn.edit'), edit_course_path(@course), class: "btn btn-primary" %> |
<%= link_to t('views.btn.back'), courses_path, class: "btn btn-default" %>
